# Issues, PR descriptions, review comments, commit messages.
# Thin: Slop rules only. Structure is enforced by the github-writing skill text.
StylesPath = styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = Slop
